# Non-secret defaults — credentials are saved via the Settings UI, not here.
# Copy this file to .env:  cp .env.example .env
# (start.sh does this automatically on first run)

# How often to re-check each product in seconds (default: 30)
CHECK_INTERVAL=30
